Steps to install and run the project

$ npm i
$ npm run dev
$ json-server --watch db.json --port 5000

Available Pages and Features
    - Homepage
    - LoginPage
Functionalities: 
    - User login and protected access to the Driver List page (only accessible after logging in).
    - All forms on the Homepage are currently functional; however, Step 3 in Section 1 is missing data, so the flow stops at Step 2.
    - Form data validation is implemented.
    - Not yet responsive on mobile devices